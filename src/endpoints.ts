export class Endpoints {
  static ROOT = 'api/v1';
  static AUTH = `${Endpoints.ROOT}/auth`;
  static USER = `${Endpoints.ROOT}/user`;
  static BRANCH = `${Endpoints.ROOT}/branch`;
  static ROLE = `${Endpoints.ROOT}/role`;
  static PERMISSION = `${Endpoints.ROOT}/permission`;
}
